# Build artifacts
/target/

# Badges generated by `badgery build` while experimenting in-repo (any
# depth: the Quickstart's `cd examples && badgery build` writes examples/badges/)
badges/

# Editor and OS noise
.DS_Store
*.swp
.idea/
.vscode/
